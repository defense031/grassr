% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asymmetry.R
\name{check_rater_asymmetry}
\alias{check_rater_asymmetry}
\title{Per-rater Se/Sp asymmetry diagnostic}
\usage{
check_rater_asymmetry(
  se,
  sp,
  rater = NULL,
  threshold_caution = 0.05,
  threshold_unsafe = 0.1,
  summary = c("max", "mean")
)
}
\arguments{
\item{se}{Numeric vector, one per rater. Per-rater sensitivity estimates
in \verb{[0, 1]}.}

\item{sp}{Numeric vector, one per rater. Per-rater specificity estimates
in \verb{[0, 1]}. Same length as \code{se}.}

\item{rater}{Optional character vector of rater labels, same length as
\code{se}. Defaults to \code{"R1"}, \code{"R2"}, ... .}

\item{threshold_caution}{Boundary between Tier 1 (\code{ok}) and Tier 2
(\code{caution}). Default \code{0.05}.}

\item{threshold_unsafe}{Boundary between Tier 2 (\code{caution}) and Tier 3
(\code{unsafe}). Default \code{0.10}.}

\item{summary}{How to reduce per-rater gaps to the scalar \code{delta_hat}.
\code{"max"} (default, conservative tripwire — any single rater above the
threshold triggers escalation) or \code{"mean"} (panel-average asymmetry).
The framework uses \code{"max"}; \code{"mean"} is provided for sensitivity
checks.}
}
\value{
An S3 object of class \code{grass_asymmetry} with fields:
\itemize{
\item \code{per_rater}: data.frame of \code{rater}, \code{se}, \code{sp}, and \verb{gap = |se - sp|}
\item \code{delta_hat}: the scalar \code{delta_hat} summary
\item \code{summary}: which summary statistic was used (\code{"max"} or \code{"mean"})
\item \code{tier}: integer tier (\code{1}, \code{2}, or \code{3})
\item \code{regime}: character regime label (\code{"ok"}, \code{"caution"}, or \code{"unsafe"})
\item \code{thresholds}: named list of the caution and unsafe cutoffs
}
}
\description{
\code{check_rater_asymmetry()} is the rater-level companion to
\code{\link[=check_asymmetry]{check_asymmetry()}}. It takes per-rater sensitivity and specificity
estimates, computes the per-rater gap \verb{|Se_j - Sp_j|}, reduces them to a
scalar \verb{delta_hat = mean_norm |Se_j - Sp_j|} (or \code{max}), and assigns the
three-tier model-safety regime that governs whether \code{q_hat} (the GRASS
operating-quality projection onto the Se = Sp diagonal) is trustworthy
as the primary summary.
}
\details{
This function is appropriate when the user already has per-rater
Se/Sp estimates — typically from a Hui-Walter / Dawid-Skene latent-class
fit (see \code{\link[=latent_class_fit]{latent_class_fit()}}), a simulation with known truth, or a
reference-standard comparison. It is the function called inside the
divergent branch of the new \code{\link[=check_asymmetry]{check_asymmetry()}} / \code{grass_report()}
workflow when per-rater (Se, Sp) become available.

For ratings-matrix input (when only the N x k binary ratings are
available, no per-rater Se/Sp), use \code{\link[=check_asymmetry]{check_asymmetry()}} instead — it
computes the cross-coefficient percentile spread without requiring
identifiability of per-rater (Se, Sp).

Tier thresholds follow the three-tier architecture:
\itemize{
\item \strong{Tier 1 — \code{ok}} (\code{delta_hat < 0.05}): diagonal default. Report
\verb{q_hat +/- SE} plus EDR and EMR_panel without per-rater disclosure.
\item \strong{Tier 2 — \code{caution}} (\verb{0.05 <= delta_hat < 0.10}): diagonal +
diagnostic. Report \verb{q_hat +/- SE} with a caution flag and the
\code{delta_hat} value.
\item \strong{Tier 3 — \code{unsafe}} (\code{delta_hat >= 0.10}): full latent-class.
\code{q_hat} is withheld as primary; report per-rater \verb{(Se_j, Sp_j)} via a
Hui-Walter / Dawid-Skene fit. PABAK's prevalence-flatness was
conditional on \code{Se = Sp}; at Tier 3 that condition is visibly broken.
}

\code{se} and \code{sp} are not identifiable from a single 2-rater binary table
without external ground truth. Supply them from: (a) a simulation with
known truth, (b) a Hui-Walter / Dawid-Skene latent-class fit under
\code{k >= 3} and prevalence heterogeneity, or (c) a reference-standard
comparison. Passing raw \code{table()} off-diagonals as \code{se} / \code{sp} is
\strong{wrong} and the function cannot detect the error.
}
\examples{
# Tier 1: symmetric raters — safe to use q_hat as primary
check_rater_asymmetry(se = c(0.86, 0.88, 0.84), sp = c(0.85, 0.87, 0.86))

# Tier 2: one rater pressing the Se = Sp assumption
check_rater_asymmetry(se = c(0.90, 0.88, 0.92), sp = c(0.82, 0.86, 0.85))

# Tier 3: within-rater Se-favoring regime — requires latent-class fit
check_rater_asymmetry(se = c(0.95, 0.93, 0.94), sp = c(0.78, 0.80, 0.79))
}
\seealso{
\code{\link[=check_asymmetry]{check_asymmetry()}} for the ratings-matrix companion (panel-level
percentile spread). \code{\link[=latent_class_fit]{latent_class_fit()}} for the recommended source of
\verb{(se, sp)} estimates.
}
