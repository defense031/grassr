% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-paragraph.R
\name{grass_methods}
\alias{grass_methods}
\title{Manuscript-ready methods paragraph for a grass result}
\usage{
grass_methods(
  result,
  format = c("markdown", "latex", "plain"),
  digits = 2,
  ...
)
}
\arguments{
\item{result}{A \code{grass_result} object (from \code{\link[=grass_report]{grass_report()}}).}

\item{format}{One of \code{"markdown"} (default), \code{"latex"}, or
\code{"plain"}. Controls emphasis markers and a small amount of
mathematical notation.}

\item{digits}{Number of decimal places for reported numerics.
Default 2 for main numbers (agreement coefficients), 3 for
derived indices (PI, BI).}

\item{...}{Unused. Reserved for family-specific extensions.}
}
\value{
A single character string. Newlines are literal \verb{\\n}; the
output can be pasted into an \code{.Rmd} or \code{.tex} document directly.
}
\description{
Generates a GRRAS-compliant (Guidelines for Reporting Reliability and
Agreement Studies) methods paragraph pre-filled with the study's
numbers. The paragraph describes the inter-rater agreement analysis
precisely enough to drop into a manuscript methods section.
}
\section{Extension contract}{


\code{grass_methods()} is an S3 generic dispatching on
\code{result$spec$family}. Each metric family implements its own method
(\code{grass_methods.grass_spec_binary}, and — in future releases —
\code{grass_methods.grass_spec_ordinal}, \code{.grass_spec_multirater},
\code{.grass_spec_continuous}). Every method must return a single
character string and honour the \code{format} argument.
}

\section{Deprecated}{

\code{grass_methods()} is deprecated in grass 0.2.0. The Target-2 reporting
workflow does not generate a regime/PI-BI methods paragraph; the
published methods text now references \code{position_on_surface()} and
\code{check_asymmetry()} directly. See \code{vignette("reporting-card")} and
\code{\link[=grass_report]{grass_report()}}.
}

\examples{
tab <- matrix(c(88, 10, 14, 88), nrow = 2,
              dimnames = list(R1 = c("0","1"), R2 = c("0","1")))
r <- grass_report(tab, format = "matrix")
cat(grass_methods(r))
}
