% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{grass_power}
\alias{grass_power}
\title{Plan a rating study: what a design can show}
\usage{
grass_power(
  metric,
  q = NULL,
  q0 = NULL,
  target = NULL,
  pi_hat = NULL,
  prevalence = NULL,
  k = NULL,
  N = NULL,
  power = NULL
)
}
\arguments{
\item{metric}{One of \code{"pabak"}, \code{"fleiss_kappa"}, \code{"mean_ac1"}, \code{"icc"}.}

\item{q}{Panel quality, the probability of a correct call on the
\code{Se = Sp} diagonal, in \verb{[0.55, 0.99]}.}

\item{q0}{The lower edge of the quality resolution the study is planned
for, in \verb{[0.55, 0.99]}: the study is sized to show that a panel of
quality \code{q} is above \code{q0}. Give \code{q0} or \code{target}, not both.}

\item{target}{A fixed coefficient value to reach. Give \code{q0} or \code{target},
not both.}

\item{pi_hat}{Observed positive rate, the share of all \verb{N x k} ratings
that are positive, in \verb{[0.05, 0.95]}. This is what the card measures
and what the surfaces are indexed by. Give \code{pi_hat} or \code{prevalence},
not both.}

\item{prevalence}{True positive rate of the finding, in \verb{[0.01, 0.99]}.
A planner usually has this rather than \code{pi_hat}. Under the symmetric
reference model a panel of quality \code{q} turns it into
\code{pi_hat = prevalence * q + (1 - prevalence) * (1 - q)}, and the
function does that conversion, at each candidate \code{q} when \code{q} is the
one being solved. The result carries both rates. A \code{pi_hat} that no
panel of quality \code{q} can produce at any prevalence is refused.}

\item{k}{Number of raters. Snaps to the nearest calibrated rater count
(2, 3, 5, 8, 15, 25), as the surfaces do everywhere.}

\item{N}{Number of subjects in \verb{[15, 1000]}.}

\item{power}{Probability, in \verb{(0, 1)}.}
}
\value{
An object of class \code{grass_power}: the design quantities with the
solved one filled in, \code{solved} naming it, \code{mode} (\code{"quality"} or
\code{"value"}), \code{feasible}, \code{reason} (when not feasible), \code{expected} (the
median coefficient a panel of quality \code{q} produces at the design, in
\code{"value"} mode), \code{curve} (power across the solved variable's range,
the data \code{plot()} draws), and \code{notes} from the surface lookup.
}
\description{
\code{grass_power()} plans a study's size. Assume a prevalence and a rough
rater quality, and it says what a design of a given size can show
about the panel, and how many more subjects or raters it would take to
show more. The result describes the design, how much a study of that
size can learn about its raters.
}
\details{
A study sized to show that a panel of quality \code{q} is above \code{q0}
returns a 95\% consistency band on quality whose lower edge sits above
\code{q0}. \code{q0} sets the resolution of the plan. A panel weaker than
assumed returns a band that sits lower and is about as wide, so the
study reports the quality it finds at the precision it was planned
for. The convention follows \code{\link[stats:power.t.test]{stats::power.t.test()}}. Fix four of \code{q},
the positive rate (\code{prevalence} or \code{pi_hat}), \code{k}, \code{N}, \code{power}, leave
one \code{NULL}, and the function solves for it.

Whether more subjects or more raters raises power depends on
prevalence. At balanced prevalence a few more raters do the work of
subjects. At a rare or very common finding they do not, because a
small sample holds only a handful of the minority class, and the
answer is more subjects.

The function also accepts \code{target}, a fixed coefficient value, for a
threshold imposed from outside (a journal's or regulator's band). A
fixed coefficient value means something different at each design.
When the value a panel of quality \code{q} produces at the design sits
below \code{target}, power falls with \code{N}. Give \code{q0} or \code{target}, not both.
}
\section{How it is computed}{

Every quantity is a direct read of the quality sweep that
\code{\link[=position_on_surface]{position_on_surface()}} returns, \code{p(q) = P(coefficient <= c | q, design)}.
Nothing is simulated at call time. For \code{q0}, by test inversion the lower
end of the band is above \code{q0} exactly when the observed coefficient exceeds
the 97.5th percentile of the \code{q0} distribution, \code{c0}, so
\code{power = 1 - p_q(c0)}. For \code{target}, \code{power = 1 - p_q(target)}.
}

\section{Solving for \code{N} or \code{k}}{

The smallest value on the calibrated surface at which \code{power} is
reached. Below about 25 subjects the curve can step or dip between
adjacent sample sizes, because the coefficient takes few distinct
values there. When none reaches it the result carries \code{NA},
\code{feasible = FALSE}, and the reason, including the best power any
design on the surface reaches.
}

\section{Solving for the positive rate}{

Leave both \code{pi_hat} and \code{prevalence} \code{NULL}. The result is the range of
observed positive rates over which \code{power} is reached, \code{solution} holds
its two endpoints, and \code{prevalence} holds the same range converted at
\code{q}.
}

\examples{
# Raters assumed near quality 0.90, prevalence 10\%, three raters: how
# many subjects to show the panel is above quality 0.80, 80\% power?
pw <- grass_power("fleiss_kappa", q = 0.90, q0 = 0.80, prevalence = 0.10,
                  k = 3, power = 0.80)
pw
if (requireNamespace("ggplot2", quietly = TRUE)) plot(pw)

# Over what positive rates does a fixed design keep that power?
grass_power("fleiss_kappa", q = 0.90, q0 = 0.80, k = 5, N = 100,
            power = 0.80)

# Against a fixed coefficient value imposed from outside:
grass_power("fleiss_kappa", target = 0.61, q = 0.90, prevalence = 0.50,
            k = 5, N = 200)
}
