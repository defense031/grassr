% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{grass_reference}
\alias{grass_reference}
\title{Look up prevalence-conditioned reference values for agreement metrics}
\usage{
grass_reference(prevalence, reference_level = 0.85, quality = NULL)
}
\arguments{
\item{prevalence}{Numeric in \verb{[0, 1]}. Values outside \verb{[0.01, 0.99]} are
clamped with a warning.}

\item{reference_level}{One of \code{0.70}, \code{0.80}, \code{0.85}, \code{0.90}. Default
\code{0.85}.}

\item{quality}{Deprecated alias. \code{"high"} → \code{reference_level = 0.85},
\code{"medium"} → \code{0.70}. Emits a one-time soft deprecation warning.}
}
\value{
A \code{grass_reference} S3 object containing a 3-row data.frame
(one row per metric) with columns \code{metric}, \code{reference}, \code{J},
\code{reference_level}, \code{quality}, \code{prevalence}. The \code{quality} column
retains the legacy \code{"high"}/\code{"medium"} label for 0.85 and 0.70 bands
and is \code{NA_character_} for the new 0.80 / 0.90 bands.
}
\description{
Returns the prevalence-conditioned reference values for Cohen's kappa,
PABAK, and Gwet's AC1 at a given prevalence and a chosen rater-quality
band. The reference is the analytical Youden-J-optimal metric value on
the Se = Sp diagonal at the chosen \code{reference_level}, under conditional
independence of the two raters given the true class.
}
\details{
In the paper these are called the \emph{prevalence-conditioned thresholds}.
The package uses the name \code{reference} because its API reports the
values alongside prevalence index and bias index rather than applying
them as cutoffs.
}
\section{Deprecated}{

\code{grass_reference()} is deprecated in grass 0.2.0. The percentile machinery
in \code{\link[=position_on_surface]{position_on_surface()}} and the new \code{\link[=grass_report]{grass_report()}} supersede the
fixed reference-curve lookup. See \code{vignette("reporting-card")} for the
v0.2.0 surface-position workflow.
}

\examples{
grass_reference(0.5)
grass_reference(0.1, reference_level = 0.70)
}
