% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{grass_report}
\alias{grass_report}
\title{Generate a GRASS Report Card from a rating matrix}
\usage{
grass_report(
  ratings,
  axis = c("inter", "intra"),
  metric = "auto",
  occasion = NULL,
  bootstrap_B = 1000L,
  bootstrap_delta_B = 0L,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{ratings}{User input: an \verb{N x k} binary matrix, an \verb{N x k} data.frame
whose columns are 0/1 / logical / 2-level factor, or a list of two
equal-length 0/1 vectors (\code{k = 2} paired form). See
\code{?normalize_ratings} for accepted shapes. Rows must be independent
subjects, one row each: the calibration assumes every row is a new
subject, and stacking repeated measurements of one subject as rows
overstates the effective sample size (analyze one card per occasion
instead; \code{vignette("grassr")}, section "The data").}

\item{axis}{One of \code{"inter"} (default) or \code{"intra"}. Selects the surface
family. The intra-axis path uses \code{occasion} to identify viewings.}

\item{metric}{One of \code{"auto"} (default; calls \code{pick_primary_coefficient()}
per Table 2), \code{"pabak"}, \code{"ac1"}, \code{"fleiss_kappa"},
\code{"icc"}. Selects which coefficient is the headline in the printed Report
Card; the full panel is always populated. (Krippendorff's alpha left
the Report Card panel at v0.6.0; it coincides with Fleiss' kappa in
the binary fully-crossed case. Use \code{\link[=obs_krippendorff_alpha]{obs_krippendorff_alpha()}} or
\code{\link[=position_on_surface]{position_on_surface()}} to compute it manually.)}

\item{occasion}{Reserved for \code{axis = "intra"}; ignored when \code{axis = "inter"}.}

\item{bootstrap_B}{Integer; bootstrap replicates for the divergent-branch
latent-class CIs. Default \code{1000L}. Set lower for fast tests.}

\item{bootstrap_delta_B}{Integer; subject-resampling replicates for the
optional bootstrap distribution of \code{delta_hat}. Default \code{0L} (off);
values below \code{50L} are treated as off.}

\item{verbose}{Logical; emit progress messages on long calls. Default
\code{FALSE}.}

\item{...}{Reserved for future extension.}
}
\value{
An object of class \code{c("grass_card", "list")} with fields
\code{sample}, \code{coefficient}, \code{delta}, \code{panel}, \code{per_rater}, \code{surface},
\code{call}, \code{grass_version}, \code{timestamp}, \code{inputs}, \code{notes}. See the
v0.2.0 paper-alignment design doc Sec.3.1 for the full structure.

\strong{\code{coefficient}} carries the primary coefficient's \code{observed_value},
its \code{surface_percentile} (the pooled percentile -- position within the
design's achievable agreement range -- on the 0-100 scale), the
implied panel quality \code{q_hat}, and the 95\% test-inversion
\code{consistency_band} on quality (suppressed at the divergent flag).

\strong{\code{panel}} is a data.frame with one row per coefficient carrying its
\code{observed_value}, \code{surface_percentile}, consistency-band columns
(\code{band_lo}, \code{band_hi}, \code{band_open_low}, \code{band_open_high}), \code{q_hat},
\code{se_q_hat}, \code{clamped}, \code{reference_used}, and \code{in_delta_hat}.

\strong{\code{delta}} carries \code{delta_hat} (implied-quality spread, in pp of
quality), \code{delta_percentile} (its percentile on the matched null),
\code{flag}, \code{matched_null}, \code{thresholds}, and \code{thresholds_source}.

\strong{Percentile units.} \code{card$coefficient$surface_percentile} and
\code{card$panel$surface_percentile} are reported on the 0-100 scale
(e.g., \code{46.3} means the 46th percentile). The underlying
\code{position_on_surface()} returns \code{percentile} on the 0-1 fraction
scale; \code{grass_report()} multiplies by 100 to match the paper's
prose convention. The \code{print} and \code{format} methods use ordinal
notation ("46th percentile").
}
\description{
\code{grass_report()} is the headline entry point for the v0.2.0 Target-2
framework. It takes an \verb{N x k} binary rating matrix and returns a
four-field Report Card: the sample summary \verb{(k, N, pi_hat)}, the primary
coefficient and its surface position, the cross-coefficient asymmetry
diagnostic \code{delta_hat} and flag, and (when \code{flag == "divergent"}) the
per-rater latent-class fit. The full panel of coefficients, pooled
percentiles, consistency bands on quality, and reference-surface
artifacts ride along on the same object for \code{summary()},
\code{as.data.frame()}, and \code{plot()} access.
}
\details{
The body, in order:
\enumerate{
\item Normalize \code{ratings} to a canonical \verb{N x k} integer matrix \code{Y} and
derive \code{pi_hat = mean(Y)}, \code{k = ncol(Y)}, \code{N = nrow(Y)}. Validate
\code{k >= 2}; warn at \code{N < 10}; note at \code{N < 30}.
\item Compute the panel of observed coefficients
(\code{compute_panel()}, internal): at \code{k = 2}, PABAK / AC1 / Cohen's
kappa; at \code{k >= 3}, PABAK / AC1 / Fleiss kappa / ICC.
\item For each panel coefficient, position the observed value on its
DGP-calibrated reference surface via \code{\link[=position_on_surface]{position_on_surface()}}.
\item Pick the primary coefficient via Table 2 (\code{metric = "auto"}) or accept
the user's override.
\item Compute the cross-coefficient implied-quality spread \code{delta_hat}
(in pp of quality) via \code{\link[=check_asymmetry]{check_asymmetry()}} and flag \code{aligned} /
\code{caution} / \code{divergent} by \code{delta_hat}'s percentile on the matched
(k, N, q_hat) null.
\item If \code{flag == "divergent"}: run a \code{\link[=latent_class_fit]{latent_class_fit()}} (Dawid-Skene EM
at \code{k >= 3}; Hui-Walter bounds at \code{k = 2}) and attach the per-rater
\verb{(Se_j, Sp_j)} table.
\item Assemble the \code{grass_card} S3 object.
}
}
\examples{
# k >= 3 computes ICC via a glmer fit when lme4 (Suggests) is
# available; without lme4 the card degrades gracefully to the
# agreement family.
set.seed(1)
Y <- matrix(rbinom(1000, 1, 0.3), nrow = 200, ncol = 5)
card <- grass_report(ratings = Y)
card                       # print
summary(card)              # full panel + per-rater
as.data.frame(card)        # tidy long-format
}
