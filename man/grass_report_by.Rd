% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-by.R
\name{grass_report_by}
\alias{grass_report_by}
\title{Report agreement metrics across groups in a tidy frame}
\usage{
grass_report_by(data, group, ..., .cohort_col = ".cohort", .parallel = FALSE)
}
\arguments{
\item{data}{A data.frame.}

\item{group}{Grouping column. Accepts either a bare symbol (e.g.,
\code{cohort}) or a character string (e.g., \code{"cohort"}).}

\item{...}{Passed through to \code{\link[=grass_report]{grass_report()}} (e.g., \code{spec}, \code{format},
\code{positive}, \code{id_col}, \code{rater_cols}, \code{response}, \code{prevalence}).}

\item{.cohort_col}{Name of the column in the returned data.frame that
carries the group value. Default \code{".cohort"}.}

\item{.parallel}{If \code{TRUE}, run per-group reports in parallel via
\code{future.apply::future_lapply()} using the user's active
\code{future::plan()}. When enabled, a \code{progressr::progressor()} reports
progress — wrap the call in \code{progressr::with_progress({ ... })} to
render a progress bar. Both \code{future.apply} and \code{progressr} are
optional dependencies; \code{grass_report_by()} errors cleanly if either
is missing and names the install command. Default \code{FALSE} (sequential).}
}
\value{
A data.frame with one row per group. Column \code{.cohort} carries the
group value as character; remaining columns match
\code{as.data.frame(result, compact = TRUE)}.
}
\description{
Splits \code{data} by \code{group}, runs \code{\link[=grass_report]{grass_report()}} on each subset, and binds
the per-group results into a single tidy data.frame using the compact
\code{as.data.frame(..., compact = TRUE)} output (drops the multi-line
\code{regime_note} that wraps poorly in bound frames).
}
\section{Deprecated}{

\code{grass_report_by()} is deprecated in grass 0.2.0. The new
\code{grass_report(ratings = Y)} headline API takes a single rating matrix and
returns a \code{grass_card}; for cohort-by-cohort analysis, loop over
subsets and bind the resulting cards via \code{do.call(rbind, lapply(parts, function(Y) as.data.frame(grass_report(ratings = Y))))}.
}

\examples{
set.seed(1)
df <- data.frame(
  xray_id = sprintf("CXR-\%03d", 1:60),
  rater_A = sample(c("abnormal","normal"), 60, replace = TRUE),
  rater_B = sample(c("abnormal","normal"), 60, replace = TRUE),
  cohort  = rep(c("site1","site2","site3"), each = 20)
)
grass_report_by(df, cohort, id_col = "xray_id", positive = "abnormal")
\dontrun{
future::plan(future::multisession, workers = 2)
progressr::with_progress({
  grass_report_by(df, cohort, id_col = "xray_id",
                  positive = "abnormal", .parallel = TRUE)
})
}
}
