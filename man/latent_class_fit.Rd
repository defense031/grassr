% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_class.R
\name{latent_class_fit}
\alias{latent_class_fit}
\title{Latent-class fit: per-rater Sensitivity and Specificity from a binary
rating matrix}
\usage{
latent_class_fit(
  ratings,
  B = 1000L,
  method = NULL,
  max_iter = 1000L,
  tol = 1e-06,
  seed = NULL,
  ...
)
}
\arguments{
\item{ratings}{An N x k binary rating matrix. Rows are subjects,
columns are raters, values in {0, 1}. Data.frame and list-of-rater-
columns inputs are coerced. Requires N >= 10, k >= 2, no NA, no
all-constant columns.}

\item{B}{Integer >= 0. Number of nonparametric bootstrap replicates.
\code{B = 0} skips the bootstrap and returns the EM point estimate (or
bounds at k = 2) without CIs.}

\item{method}{One of \code{"dawid_skene_em"}, \code{"hui_walter"}, or \code{NULL}
(default). When \code{NULL}, dispatches to Hui-Walter at k = 2 and to
Dawid-Skene EM at k >= 3. Supplying \code{"dawid_skene_em"} at k = 2
errors -- the EM is unidentified there.}

\item{max_iter}{Integer. EM iteration cap. Default 1000.}

\item{tol}{Numeric. EM log-likelihood tolerance. Default 1e-6.}

\item{seed}{Optional integer. Sets the bootstrap RNG; the EM itself
is deterministic.}

\item{...}{Reserved for future extensions; currently ignored.}
}
\value{
An S3 object of class \code{c("grass_latent_class", "list")}:
\itemize{
\item \code{per_rater}: data.frame with \code{rater}, \code{se_hat}, \code{sp_hat} (NA at
k = 2), \code{se_lower}, \code{se_upper}, \code{sp_lower}, \code{sp_upper},
\code{bound_only} (TRUE at k = 2).
\item \code{method}: \code{"dawid_skene_em"} or \code{"hui_walter"}.
\item \code{converged}: logical (NA at k = 2).
\item \code{iterations}: integer (NA at k = 2).
\item \code{B}: integer; bootstrap replicates run.
\item \code{prevalence_hat}: numeric estimated prevalence (NA at k = 2).
\item \code{log_likelihood}: numeric (NA at k = 2).
}
}
\description{
\code{latent_class_fit()} is the divergent-branch fallback for the GRASS
Reporting Card. When the cross-coefficient panel disagrees, the
framework abandons the single q_hat summary and reports per-rater
(Se_j, Sp_j) instead. This function fits those per-rater accuracy
parameters from an N x k binary rating matrix.
}
\details{
Two regimes, dispatched by \code{k}:
\itemize{
\item \strong{k >= 3: Dawid-Skene 1979 expectation-maximization.} Latent binary
class C_i in {0, 1}; conditional independence of raters given C;
parameters (pi, Se_j, Sp_j) for j = 1..k. Initialized from
majority-vote consensus. Iterated to log-likelihood tolerance \code{tol}
or \code{max_iter}. Hand-rolled in base R, no new package dependency.
\item \strong{k == 2: Hui-Walter 1980 inequality bounds.} Per-rater Se and Sp
are NOT point-identified from a single 2x2 table without external
information (e.g., a known-prevalence subgroup or a reference
standard). We return the inequality bounds the observed marginals
support: with rater j positive rate \code{P_j}, \code{Se_j} is bounded below
by \code{P_j}, \code{Sp_j} is bounded below by \code{1 - P_j}, both above by 1.
The returned rows have \code{bound_only = TRUE} and \code{se_hat = sp_hat = NA}. This is a documented limitation of the design, not a bug.
}

Bootstrap CI (when \code{B > 0}): nonparametric, subjects-with-replacement.
At k >= 3 the EM is refit on each bootstrap sample; at k = 2 the
bounds are recomputed and the bootstrap distribution is on the
bound midpoints. Reports the empirical 2.5 / 97.5 percentiles on
the per-rater Se_j and Sp_j (or their bound midpoints at k = 2).
}
\examples{
\dontrun{
set.seed(1)
N <- 500; k <- 5
Se <- 0.90; Sp <- 0.85; pi <- 0.30
C <- rbinom(N, 1, pi)
Y <- matrix(0L, N, k)
for (j in seq_len(k))
  Y[, j] <- rbinom(N, 1, ifelse(C == 1, Se, 1 - Sp))
fit <- latent_class_fit(Y, B = 200, seed = 1)
print(fit)
}

}
\references{
Dawid, A. P. and Skene, A. M. (1979). Maximum likelihood estimation
of observer error-rates using the EM algorithm. \emph{Applied Statistics},
28(1), 20--28.

Hui, S. L. and Walter, S. D. (1980). Estimating the error rates of
diagnostic tests. \emph{Biometrics}, 36(1), 167--171.
}
