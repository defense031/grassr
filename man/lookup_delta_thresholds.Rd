% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delta_thresholds.R
\name{lookup_delta_thresholds}
\alias{lookup_delta_thresholds}
\title{Look up calibrated cross-coefficient asymmetry thresholds at (k, N)}
\usage{
lookup_delta_thresholds(k, N)
}
\arguments{
\item{k}{Integer panel size (number of raters).}

\item{N}{Integer panel sample size.}
}
\value{
A list with
\describe{
\item{thresholds}{Length-2 numeric in pp, named c("caution", "divergent").}
\item{source}{One of \code{"calibrated_at_k_N"},
\code{"snapped_to_nearest_calibrated"}, or \code{"default_fallback"}. The
last is reserved for the (impossible-with-bundled-sysdata) case
of an empty table.}
\item{note}{Human-readable explanation suitable for the card's notes.
Empty string when source = \code{"calibrated_at_k_N"}.}
\item{snapped_k}{The k value actually used (NA when no snap was needed).}
\item{snapped_N}{The N value actually used (NA when no snap was needed).}
}
}
\description{
Returns the per-(k, N) calibrated \verb{(caution, divergent)} threshold pair
from the bundled \code{delta_thresholds_lookup} table, or the default
modal-design pair \code{c(9.25, 11.75)} when no calibrated entry is available.
Always returns the source so callers can surface the choice in
\code{grass_report()} output.
}
\keyword{internal}
