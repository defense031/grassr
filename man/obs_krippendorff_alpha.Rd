% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-multi-rater.R
\name{obs_krippendorff_alpha}
\alias{obs_krippendorff_alpha}
\title{Krippendorff's alpha (nominal, binary, fully crossed)}
\usage{
obs_krippendorff_alpha(Y)
}
\arguments{
\item{Y}{N x k integer matrix in \{0L, 1L\}.}
}
\value{
Single numeric.
}
\description{
Fully-crossed design (\code{m_i = k} for all i). Hayes-Krippendorff coincidence-
matrix reduction:
\verb{o_\{01\} = 2 * sum_i r_i * (k - r_i) / (k - 1)},
\code{n_0 = N*k - sum(r_i)}, \code{n_1 = sum(r_i)}, \code{n_total = N*k},
\verb{alpha = 1 - (n_total - 1) * o_\{01\} / (2 * n_0 * n_1)}.
}
\details{
Returns \code{NA_real_} (with a \code{note} attribute) if the design is degenerate
(no positives or no negatives in \code{Y}).

As of v0.6.0, alpha is not part of the Report Card panel or the
\code{delta_hat} coefficient set: in the binary fully-crossed case it
coincides with Fleiss' kappa asymptotically (median absolute difference
0.00024 across the 10,140-cell calibration grid). This function is
exported for users who need the value for cross-study comparison.
}
