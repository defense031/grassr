% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.grass_result}
\alias{plot.grass_result}
\title{Plot a grass report}
\usage{
\method{plot}{grass_result}(
  x,
  type = c("landing", "regime"),
  labels = c("auto", "inline", "legend"),
  show_medium = FALSE,
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{grass_result} object.}

\item{type}{Either \code{"landing"} (default) or \code{"regime"}.}

\item{labels}{Where to identify the three curves: \code{"auto"} (default --
inline at the right edge unless curves are too close together),
\code{"inline"}, or \code{"legend"}.}

\item{show_medium}{If \code{TRUE}, also plot the medium-quality reference
curves as a faint dotted band. Default \code{FALSE}.}

\item{title, subtitle}{Optional strings to override the default title and
subtitle. Pass \code{NULL} to omit either.}

\item{...}{Unused.}
}
\value{
A ggplot object.
}
\description{
The default \code{"landing"} plot overlays observed kappa, PABAK, and AC1 on
the GRASS prevalence-conditioned reference curves. The \code{"regime"} plot
places the study on the PI^2 vs BI^2 plane.
}
\details{
Reference curves are the Youden-J-optimal metric values from the GRASS
simulation across prevalence, for competent raters (Se, Sp at or above
0.85 for \code{"high"} quality; 0.70 for \code{"medium"}).
}
