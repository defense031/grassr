% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-card.R
\name{plot_surface}
\alias{plot_surface}
\title{Standalone reference-surface plot for prospective study design}
\usage{
plot_surface(
  metric,
  pi_hat = NULL,
  observed = NULL,
  k = NULL,
  N = NULL,
  axis = c("inter", "intra"),
  bands = c(0.5, 0.625, 0.75, 0.875, 1),
  ...
)
}
\arguments{
\item{metric}{Character scalar. One of \code{"pabak"}, \code{"mean_pabak"},
\code{"fleiss_kappa"}, \code{"kappa"}, \code{"mean_ac1"}, \code{"ac1"},
\code{"krippendorff_a"}. ICC is not supported (see Details).}

\item{pi_hat}{Optional numeric scalar in \verb{(0, 1)}. The design's
marginal positive rate. If supplied alone, draws a vertical
reference line on the surface; if supplied together with
\code{observed}, anchors the pin point.}

\item{observed}{Optional numeric scalar. An observed (or hypothetical)
coefficient value. When supplied with \code{pi_hat}, the function
inverts to \verb{hat q} and pins a marker at \verb{(pi_hat, hat q)}.}

\item{k}{Optional integer. Rater count. Used for the subtitle only;
non-ICC closed-form surfaces are k-invariant.}

\item{N}{Optional integer. Sample size. Used for the subtitle only;
non-ICC closed-form surfaces are N-invariant in the large-N limit
the surface represents.}

\item{axis}{One of \code{"inter"} (default) or \code{"intra"}. Used for the
subtitle only.}

\item{bands}{Numeric length-5 vector giving band boundaries on \code{q}.
Default \code{c(0.5, 0.625, 0.75, 0.875, 1.0)}.}

\item{...}{Reserved for future extension.}
}
\value{
A \code{ggplot} object.
}
\description{
Renders the same context-conditioned reference surface that
\code{plot.grass_card(type = "surface")} shows, but driven by scalar inputs
rather than a fitted \code{\link[=grass_report]{grass_report()}} result. The intended use case is
prospective study design: a practitioner planning a multi-rater study at
design \verb{(pi_hat, k, N)} can ask "what does my reference surface look
like for this metric, before I collect data?" — and \code{plot_surface()}
answers without requiring a rating matrix.
}
\details{
The plot is a heatmap of the closed-form expectation
\code{E[metric](M_1, q)} over the surface, where \code{M_1} is the marginal
positive rate and \verb{q in [0.5, 1]} is the diagonal rater operating
quality. Dotted contours overlay the four-band partition
(\code{bands} argument). When \code{pi_hat} is supplied alone, a dashed vertical
line marks the design's marginal. When both \code{pi_hat} and \code{observed}
are supplied, a filled marker pins the implied \verb{(M_1, hat q)} point on
the surface; \verb{hat q} is recovered by closed-form inversion of \code{observed}
against the reference curve at \code{pi_hat}.

This function uses the closed-form non-ICC surfaces only (PABAK,
Fleiss kappa, mean-pairwise AC1, Krippendorff alpha). ICC surfaces
depend on the full F-shape and the GLMM-gap-corrected fitted reference;
those are accessed via \code{\link[=position_on_surface]{position_on_surface()}} with \code{metric = "icc"}
and (if available) a fitted card via \code{plot.grass_card(type = "surface")}.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  # Bare surface — what does PABAK's reference look like?
  plot_surface("pabak")

  # Mark a design context (no observed value yet — pre-data).
  plot_surface("fleiss_kappa", pi_hat = 0.30, k = 5, N = 200)

  # Pin a hypothetical observation on the surface.
  plot_surface("mean_ac1", pi_hat = 0.30, observed = 0.62,
               k = 5, N = 200)
}
}
}
\seealso{
\code{\link[=plot.grass_card]{plot.grass_card()}} for the card-driven surface view that
adds card-specific annotations (band, qualifier, primary-coefficient
pin); \code{\link[=position_on_surface]{position_on_surface()}} for the underlying inversion machinery.
}
